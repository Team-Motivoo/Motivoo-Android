<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="soreSpotType"
            type="sopt.motivoo.presentation.type.SoreSpotType" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/cl_sore_spot_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/selector_onboarding_button_background">

        <TextView
            android:id="@+id/tv_sore_spot_type_title"
            style="@style/B5_med"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingVertical="19dp"
            android:text="@{soreSpotType.titleRes}"
            android:textColor="@color/gray_800_303031"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="목" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>